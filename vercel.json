{
  "buildCommand": "pnpm --filter front build",
  "outputDirectory": "front/dist",
  "installCommand": "pnpm install",
  "devCommand": "pnpm --filter front dev --port $PORT",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
